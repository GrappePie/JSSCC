# Exact GXSCC B236E browser engine

The GitHub Pages branch now has a clean-room engine path based on tables recovered from the original B236E executable. The recovered numeric data is kept separate from the browser playback code so future reverse-engineering can refine behavior without replacing verified tables.
